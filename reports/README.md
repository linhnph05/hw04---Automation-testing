# HW03 Evidence Workspace

Scenario B is tested on B1 (events listing), B2 (event detail), and B3 (registration roles). The report files are completed from live test observations only. BrowserStack credentials and raw JSON responses are intentionally excluded.
